---
interface Props {
  href: string;
  isActive?: boolean;
  isMobile?: boolean;
}
const { href, isActive, isMobile } = Astro.props;
---

<a
  href={href}
  class="rounded-md px-4 py-2 text-sm font-medium transition-colors hover:bg-blue-50 hover:text-blue-600 active:bg-blue-100 active:text-blue-800 sm:text-base"
  class:list={[
    { "bg-blue-100 font-medium text-blue-800 shadow-sm dark:bg-slate-50 dark:text-blue-800": isActive },
    { block: isMobile },
  ]}
  aria-current={isActive ? "page" : undefined}><slot /></a
>
