---
import FullWidthBlock from "./content/FullWidthBlock.astro";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<FullWidthBlock withContainer class="!p-4 text-center" class:list={className}>
  <slot />
</FullWidthBlock>
