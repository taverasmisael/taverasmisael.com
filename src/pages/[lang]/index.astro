---
import { type Language, isSupportedLang } from "@/utils/i18n";

const { lang } = Astro.params as { lang: Language };

if (!isSupportedLang(lang)) {
  return Astro.redirect(`404?from=${lang as string}`);
}
---
